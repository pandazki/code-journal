# dependencies & build output
node_modules/
dist/
test-dist/
*.tsbuildinfo
.tsup/

# generated plugin CLI bundle (produced by `npm run sync-plugin-bin`)
claude-plugin/bin/code-journal.js
claude-plugin/bin/code-journal.js.map

# server runtime data
server/data/

# code-journal local state (when a dev runs the tool inside the repo)
.code-journal/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Experiment artifacts that contain raw transcript content — kept out of source control.
# The committed `report.md` is aggregate-only with no verbatim quotes.
experiments/observation-lens-v1/digests/
experiments/observation-lens-v1/events/
experiments/observation-lens-v1/audits/
experiments/observation-lens-v1/PROJECT-MAPPING.md
